[Unit]
Description=Grouproxy node monitor
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=grouproxy
Group=grouproxy
CapabilityBoundingSet=CAP_NET_BIND_SERVICE CAP_NET_ADMIN
AmbientCapabilities=CAP_NET_BIND_SERVICE CAP_NET_ADMIN
ExecStartPre=/opt/grouproxy/bin/grouproxy-monitor -config /opt/grouproxy/etc/monitor.yaml -validate
ExecStart=/opt/grouproxy/bin/grouproxy-monitor -config /opt/grouproxy/etc/monitor.yaml
Restart=on-failure
RestartSec=5s
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/grouproxy/etc /opt/grouproxy/var

[Install]
WantedBy=multi-user.target
