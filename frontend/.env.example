# Leave empty when Next.js serves the dashboard and proxies /api to the backend.
NEXT_PUBLIC_API_BASE_URL=
GROUPROXY_BACKEND_API_URL=http://127.0.0.1:8000
# scripts/dev-frontend.sh exposes the dashboard directly on :80 by default.
# Override GROUPROXY_FRONTEND_PORT only for local development or test runs.
